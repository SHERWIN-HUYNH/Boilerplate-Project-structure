# Frontend

Next.js frontend boilerplate for AI interview projects.

## Run locally

```bash
npm install
npm run dev
```

## Run with Docker

1. Copy `.env.example` to `.env`
2. Run:

```bash
docker compose up --build
```

The frontend will be available at `http://localhost:3000`.
