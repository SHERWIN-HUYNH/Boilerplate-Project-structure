# Backend

FastAPI backend boilerplate for AI interview projects.

## Run locally

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## API

- `GET /health`
- `GET /api/v1/health`
- `GET /api/v1/users`
- `GET /api/v1/items`
